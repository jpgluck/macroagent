{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // \"\"' | { read -r f; case \"$f\" in *.py) python3 -m py_compile \"$f\" ;; *.ts) npx tsc --noEmit ;; esac; } 2>/dev/null || true",
            "statusMessage": "Checking syntax..."
          }
        ]
      }
    ]
  }
}
